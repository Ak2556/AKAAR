import type { Metadata } from "next";
import SettingsPage from "./SettingsClient";

export const metadata: Metadata = {
  title: "Settings | AKAAR 3D",
  robots: { index: false, follow: false },
};

export default SettingsPage;
