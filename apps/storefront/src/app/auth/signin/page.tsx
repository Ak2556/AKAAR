import type { Metadata } from "next";
import SignInPage from "./SignInClient";

export const metadata: Metadata = {
  title: "Sign In | AKAAR 3D",
  robots: { index: false, follow: false },
};

export default SignInPage;
