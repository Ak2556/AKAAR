import type { Metadata } from "next";
import SignUpPage from "./SignUpClient";

export const metadata: Metadata = {
  title: "Create Account | AKAAR 3D",
  robots: { index: false, follow: false },
};

export default SignUpPage;
