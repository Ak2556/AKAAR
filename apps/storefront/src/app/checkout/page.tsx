import type { Metadata } from "next";
import CheckoutPage from "./CheckoutClient";

export const metadata: Metadata = {
  title: "Checkout | AKAAR 3D",
  robots: { index: false, follow: false },
};

export default CheckoutPage;
